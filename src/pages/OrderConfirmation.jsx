import Layout from "../components/layout/Layout";

// Owned by Timothy (feature/cart-checkout-timothy) — Figma frame
// "07 - Order Confirmation & Invoice".
export default function OrderConfirmation() {
  return (
    <Layout>
      <div style={{ padding: 80, textAlign: "center", color: "var(--color-gray)" }}>
        Order Confirmation & Invoice page — Timothy's section
      </div>
    </Layout>
  );
}
